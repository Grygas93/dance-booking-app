<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{workshop.name}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f8f9fa;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      margin: 0;
    }
    .section {
      padding: 60px 0;
    }
    footer {
      text-align: center;
      padding: 30px;
      background-color: #f1f1f1;
    }
    .btn-book {
      background-color: #8884d8;
      color: white;
      border: none;
      border-radius: 6px;
      padding: 10px 16px;
    }
    .btn-book:hover {
      background-color: #6d6ab9;
    }
    .description-box {
      background-color: #ffffff;
      padding: 15px;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
      white-space: pre-wrap;
    }
  </style>
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light py-3">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">Dance School</a>
      <div>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/classes">Weekly Classes</a></li>
          <li class="nav-item"><a class="nav-link" href="/workshops">Workshops</a></li>
          <li class="nav-item"><a class="nav-link" href="/login">Admin Login</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Workshop Details -->
  <section class="section bg-light" style="flex: 1;">
    <div class="container">
      <a href="/workshops" class="btn btn-outline-secondary mb-4">← Back to Workshops</a>
      <div class="row justify-content-center">
        <div class="col-md-8">
          <div class="card p-4 mb-4">
            <h3 class="mb-3">{{workshop.name}}</h3>
            <p><strong>Style:</strong> {{workshop.style}}</p>
            <p><strong>Level:</strong> {{workshop.level}}</p>
            <p><strong>Start Date:</strong> {{workshop.startDate}}</p>
            <p><strong>End Date:</strong> {{workshop.endDate}}</p>
            <p><strong>Time:</strong> {{workshop.time}}</p>
            <p><strong>Price:</strong> £{{workshop.price}}</p>
            <div class="mt-3">
              <strong>Description:</strong>
              <div class="description-box">{{workshop.description}}</div>
            </div>
          </div>

          <!-- Booking Form -->
          <div class="card p-4">
            <h4 class="mb-3">Book this Workshop</h4>
            <form method="POST" action="/workshops/book">
              <input type="hidden" name="workshopId" value="{{workshop._id}}">
              <div class="mb-3">
                <label class="form-label">First Name</label>
                <input type="text" name="firstName" class="form-control" required>
              </div>
              <div class="mb-3">
                <label class="form-label">Last Name</label>
                <input type="text" name="lastName" class="form-control" required>
              </div>
              <div class="mb-3">
                <label class="form-label">Email</label>
                <input type="email" name="email" class="form-control" required>
              </div>
              <button type="submit" class="btn btn-book w-100">Book a Workshop</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <p>&copy; 2025 Dance School | All rights reserved</p>
  </footer>
</body>
</html>
