<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Manage Classes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .container {
      padding: 60px 20px;
    }
    .btn-add {
      background-color: #8884d8;
      color: white;
      border: none;
      border-radius: 6px;
      padding: 10px 16px;
      text-decoration: none;
      display: inline-block;
    }
    .btn-add:hover {
      background-color: #6d6ab9;
    }
    .table th, .table td {
      vertical-align: middle;
    }
    .action-btn {
      margin-right: 6px;
    }
  </style>
</head>
<body>

  <div class="container">
    <!-- Back and Add New Class buttons -->
    <div class="d-flex justify-content-between align-items-center mb-4">
      <a href="/admin" class="btn btn-outline-secondary">← Back</a>
      <a href="/admin/classes/add" class="btn btn-add">Add New Class</a>
    </div>

    <div class="table-responsive">
      <table class="table table-bordered table-striped">
        <thead class="table-light">
          <tr>
            <th>Day</th>
            <th>Time</th>
            <th>Name</th>
            <th>Level</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#classes}}
          <tr>
            <td>{{day}}</td>
            <td>{{time}}</td>
            <td>{{name}}</td>
            <td>{{level}}</td>
            <td>
              <a href="/admin/classes/edit/{{_id}}" class="btn btn-sm btn-outline-primary action-btn">Edit</a>
              <form action="/admin/classes/delete" method="POST" class="d-inline">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure you want to delete this class?')">Delete</button>
              </form>
            </td>
          </tr>
          {{/classes}}
          {{^classes}}
          <tr><td colspan="5" class="text-center">No classes available.</td></tr>
          {{/classes}}
        </tbody>
      </table>
    </div>
  </div>

</body>
</html>
