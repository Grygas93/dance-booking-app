<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Users & Organisers</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .container {
      padding: 60px 20px;
    }
    .btn-add {
      background-color: #8884d8;
      color: white;
      border: none;
      border-radius: 6px;
      padding: 10px 16px;
      text-decoration: none;
      display: inline-block;
    }
    .btn-add:hover {
      background-color: #6d6ab9;
    }
    .action-btn {
      margin-right: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <a href="/admin" class="btn btn-outline-secondary mb-4">← Back </a>
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2 class="mb-0">Manage Users</h2>
      <a href="/admin/users/add" class="btn btn-add">+ Add New User</a>
    </div>

    <div class="table-responsive">
      <table class="table table-bordered table-striped">
        <thead class="table-light">
          <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#users}}
          <tr>
            <td>{{firstName}}</td>
            <td>{{lastName}}</td>
            <td>{{email}}</td>
            <td>{{role}}</td>
            <td>
              <a href="/admin/users/edit/{{_id}}" class="btn btn-sm btn-outline-primary action-btn">Edit</a>
              <form method="post" action="/admin/users/delete" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this user?')">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
              </form>
            </td>
          </tr>
          {{/users}}
        </tbody>
      </table>
    </div>
  </div>

  <footer class="text-center bg-light py-3 mt-auto">
    <p>&copy; 2025 Dance School | All rights reserved</p>
  </footer>
</body>
</html>
