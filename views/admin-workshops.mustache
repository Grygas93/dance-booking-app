<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Workshops</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .container {
      padding: 60px 20px;
    }
    .btn-add {
      background-color: #8884d8;
      color: white;
      border: none;
      border-radius: 6px;
      padding: 10px 16px;
      text-decoration: none;
      display: inline-block;
    }
    .btn-add:hover {
      background-color: #6d6ab9;
    }
    .table th, .table td {
      vertical-align: middle;
    }
    .action-btn {
      margin-right: 5px;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light py-3">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">Dance School</a>
      <div>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/classes">Weekly Classes</a></li>
          <li class="nav-item"><a class="nav-link" href="/workshops">Workshops</a></li>
          <li class="nav-item"><a class="nav-link" href="/login">Admin Login</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <a href="/admin" class="btn btn-outline-secondary">← Back</a>
      <a href="/admin/workshops/add" class="btn btn-add">Add New Workshop</a>
    </div>

    <div class="table-responsive">
      <table class="table table-bordered table-striped">
        <thead class="table-light">
          <tr>
            <th>Name</th>
            <th>Style</th>
            <th>Level</th>
            <th>Date</th>
            <th>Time</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#workshops}}
          <tr>
            <td>{{name}}</td>
            <td>{{style}}</td>
            <td>{{level}}</td>
            <td>{{date}}</td>
            <td>{{time}}</td>
            <td>
              <a href="/admin/workshops/edit/{{_id}}" class="btn btn-sm btn-outline-primary action-btn">Edit</a>
              <form action="/admin/workshops/delete" method="POST" class="d-inline">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure you want to delete this workshop?')">Delete</button>
              </form>
            </td>
          </tr>
          {{/workshops}}
        </tbody>
      </table>
    </div>
  </div>

  <footer class="text-center bg-light py-3 mt-auto">
    <p>&copy; 2025 Dance School | All rights reserved</p>
  </footer>
</body>
</html>
</html>
