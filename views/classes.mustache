<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Weekly Classes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f8f9fa;
    }
    .section-dark {
      background-color: #e8e8e8;
      padding: 60px 0;
    }
    .day-section {
      margin-bottom: 40px;
    }
    .class-card {
      background-color: white;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      padding: 1rem;
      height: 100%;
    }
    .btn-book {
      background-color: #8884d8;
      border: none;
      border-radius: 6px;
      padding: 10px 16px;
      color: white;
    }
    .btn-book:hover {
      background-color: #6d6ab9;
    }
    .navbar-nav .nav-link {
      font-weight: 500;
      font-size: 1rem;
      margin-left: 20px;
    }
    footer {
      text-align: center;
      padding: 30px;
      background-color: #f1f1f1;
    }
  </style>
</head>
<body>

  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light py-3">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">Dance School</a>
      <div>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/classes">Weekly Classes</a></li>
          <li class="nav-item"><a class="nav-link" href="/workshops">Workshops</a></li>
          <li class="nav-item"><a class="nav-link" href="/login">Admin Login</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <section class="section-dark">
    <div class="container">
      <h2 class="mb-5 text-center">Weekly Classes Timetable</h2>

      {{#days}}
        <div class="day-section">
          <h4 class="mb-4">{{day}}</h4>
          <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            {{#classes}}
              <div class="col">
                <div class="class-card">
                  <p><strong>Style:</strong> {{name}}</p>
                  <p><strong>Level:</strong> {{level}}</p>
                  <p><strong>Time:</strong> {{time}}</p>
                  <p><strong>Duration:</strong> {{duration}}</p>
                  <p><strong>Location:</strong> {{location}}</p>
                  <p><strong>Price:</strong> £{{price}}</p>
                  <form action="/class/{{_id}}" method="GET">
                    <button type="submit" class="btn btn-book w-100">Book</button>
                  </form>
                </div>
              </div>
            {{/classes}}
            {{^classes}}
              <p>No classes available.</p>
            {{/classes}}
          </div>
        </div>
      {{/days}}
    </div>
  </section>

  <footer>
    <p>&copy; 2025 Dance School | All rights reserved</p>
  </footer>

</body>
</html>